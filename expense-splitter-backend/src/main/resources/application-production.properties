# Production Database Configuration for Render
spring.datasource.url=jdbc:postgresql://dpg-d13plhggjchc73fecfa0-a.singapore-postgres.render.com/expense_splitter_db
spring.datasource.username=expense_splitter_db_user
spring.datasource.password=BNs8hZPCVF65lHC8vOvKiLj6cE0G9npH

# Use the PostgreSQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# In production, 'validate' is a safe choice. It checks if the schema matches the entities.
# 'update' can be used but carries risks in production.
spring.jpa.hibernate.ddl-auto=validate

# Show SQL statements in logs (optional, can be set to false for cleaner logs)
spring.jpa.show-sql=true
